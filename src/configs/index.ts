import { DEVICES } from "./devices";
import { PATH_NAME } from "./pathName";
import { USER_ROLE, USER_TEAM, USER_ROLE_SUPER_ADMIN } from "./userRole";

export const LANGUAGE = {
  ENGLISH: "en",
  VIETNAMESE: "vn",
};

export { PATH_NAME, USER_ROLE, USER_TEAM, DEVICES, USER_ROLE_SUPER_ADMIN };
